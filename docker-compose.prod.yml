version: "3"
services:
  postgres:
    image: postgres:11.6
  web:
    restart: always
    environment:
      - DJANGO_SECRET_KEY=local
      - VIRTUAL_HOST=cookiecutter.skydreamy.com
      - LETSENCRYPT_HOST=cookiecutter.skydreamy.com
    build: ./
    command: >
      bash -c "python wait_for_postgres.py &&
               ./manage.py migrate &&
               ./manage.py runserver 0.0.0.0:80"
    volumes:
      - ./:/code
    depends_on:
      - postgres
  documentation:
    restart: always
    environment:
      - VIRTUAL_HOST=cookiecutterdocs.skydreamy.com
      - LETSENCRYPT_HOST=cookiecutterdocs.skydreamy.com
    build: ./
    command: "mkdocs serve"
    volumes:
      - ./:/code

networks:
  default:
    external:
      name: nginx-proxy
